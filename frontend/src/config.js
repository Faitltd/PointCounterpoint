// API configuration
const config = {
  // Local development backend
  API_URL: 'http://localhost:5001',

  // Deployed backend (flip back for production)
  // API_URL: 'https://pointcounterpoint-backend-526297187726.us-central1.run.app',
};

export default config;
